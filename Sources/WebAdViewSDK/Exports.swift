// Re-export the pure-logic core so `import WebAdViewSDK` is all a consumer
// needs — AdLoadState, LazyLoadingConfig, ViewabilityMode, ViewabilityUpdate,
// SNLog, etc. surface without a second import.
@_exported import WebAdViewCore
